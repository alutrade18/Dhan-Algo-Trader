You are working on a large-scale corporate Algo Trading application.

Objective:
Implement a complete UI color palette system with support for:
1. Dark Theme
2. Light (Secondary) Theme

Important Constraints:
- DO NOT modify any business logic, authentication, API, or security-related code.
- ONLY update UI-related styling (colors, themes, visual consistency).
- Ensure zero functional impact on the app.

Scope:
Apply consistent theming across ALL UI elements, including but not limited to:
- Text (primary, secondary, muted, headings)
- Buttons (primary, secondary, hover, disabled states)
- Icons
- Input fields / Search boxes
- Placeholder text
- Logs / console panels
- Widgets / cards / containers
- Backgrounds (main, panels, overlays)
- Borders, dividers
- Tables and data grids
- Alerts, notifications, badges

Implementation Requirements:
1. Create a centralized theme configuration:
   - Use variables (CSS variables, Tailwind config, or theme object depending on stack)
   - Example structure:
     primary
     secondary
     background
     surface
     textPrimary
     textSecondary
     border
     success
     warning
     error
     info

2. Define TWO COMPLETE THEMES:

--- DARK THEME ---
Background: #0B0F14
Surface: #121821
Primary: #4F9CF9
Secondary: #7C3AED
Text Primary: #E6EDF3
Text Secondary: #9AA4B2
Border: #1F2937
Success: #22C55E
Warning: #F59E0B
Error: #EF4444

--- LIGHT THEME ---
Background: #F8FAFC
Surface: #FFFFFF
Primary: #2563EB
Secondary: #7C3AED
Text Primary: #0F172A
Text Secondary: #475569
Border: #E2E8F0
Success: #16A34A
Warning: #D97706
Error: #DC2626

3. Replace all hardcoded color values across the app with theme variables.

4. Ensure:
   - Proper contrast and readability
   - Consistent hover, active, and disabled states
   - Clean professional corporate trading UI look

5. Implement theme switching:
   - Global toggle (dark/light)
   - Persist user preference (localStorage or equivalent)

6. Refactor existing styles:
   - Remove duplicate or inconsistent color usage
   - Standardize spacing and visual hierarchy where needed (without changing layout structure)

7. Validate:
   - No broken UI
   - No console errors
   - All components visually consistent in both themes

Output:
- Updated UI styling code
- Centralized theme configuration
- Theme switch implementation
- Clean, maintainable, scalable structure